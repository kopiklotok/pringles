git config credential.helper 'cache --timeout=300'
git config --global user.email "hanirizo@gmail.com"
git config --global user.name "Presiden"
git add .
git commit -m 'init'
git push -u origin master
